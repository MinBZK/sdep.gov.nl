clients:
  # Short-term rental platforms (STR) - test
  - id: "sdep-str-01"
    name: "Short-term rental platform 01"
    description: "STR client with read-write access"
    secret: "sdep-str-01"
    service_account_roles:
      - sdep_str
      - sdep_read
      - sdep_write
  # Competent authorities (CA) - test
  - id: "sdep-ca-01"
    name: "Competent authority 01"
    description: "CA client with read-write access"
    secret: "sdep-ca-01"
    service_account_roles:
      - sdep_ca
      - sdep_read
      - sdep_write
  # Competent authorities (CA) - test "real"
  - id: "sdep-ca-0363"
    name: "Gemeente Amsterdam"
    description: "CA client with read-write access"
    secret: "sdep-ca-0363"
    service_account_roles:
      - sdep_ca
      - sdep_read
      - sdep_write
  - id: "sdep-ca-0599"
    name: "Gemeente Rotterdam"
    description: "CA client with read-write access"
    secret: "sdep-ca-0599"
    service_account_roles:
      - sdep_ca
      - sdep_read
      - sdep_write
  - id: "sdep-ca-0518"
    name: "Gemeente Den Haag"
    description: "CA client with read-write access"
    secret: "sdep-ca-0518"
    service_account_roles:
      - sdep_ca
      - sdep_read
      - sdep_write
