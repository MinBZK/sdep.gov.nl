m2m_clients:
  # Short-term rental platforms (STR) - test
  - id: "sdep-str-01"
    name: "STR platform 01"
    description: "M2M client (STR) with read-write access"
    secret: "sdep-str-01"
    service_account_roles:
      - sdep_str
      - sdep_read
      - sdep_write
  # Competent authorities (CA) - test
  - id: "sdep-ca-01"
    name: "CA competent authority 01"
    description: "M2M client (CA) with read-write access"
    secret: "sdep-ca-01"
    service_account_roles:
      - sdep_ca
      - sdep_read
      - sdep_write
  # Competent authorities (CA) - test "real"
  - id: "sdep-ca-0363"
    name: "Gemeente Amsterdam"
    description: "M2M client (CA) with read-write access"
    secret: "sdep-ca-0363"
    service_account_roles:
      - sdep_ca
      - sdep_read
      - sdep_write
  - id: "sdep-ca-0599"
    name: "Gemeente Rotterdam"
    description: "M2M client (CA) with read-write access"
    secret: "sdep-ca-0599"
    service_account_roles:
      - sdep_ca
      - sdep_read
      - sdep_write
  - id: "sdep-ca-0518"
    name: "Gemeente Den Haag"
    description: "M2M client (CA) with read-write access"
    secret: "sdep-ca-0518"
    service_account_roles:
      - sdep_ca
      - sdep_read
      - sdep_write
